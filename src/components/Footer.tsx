export default function Footer() {
  return (
    <footer className="hidden border-t border-white/10 bg-[#050918] sm:block">
      <div className="mx-auto max-w-6xl px-4 py-6 text-sm text-slate-400 sm:px-6">
        Student Carry is an MVP matching service for student deliveries in Europe.
        We do not collect passport numbers or ID documents at MVP stage.
        Identity verification may be introduced later through a trusted third-party provider.
      </div>
    </footer>
  );
}
